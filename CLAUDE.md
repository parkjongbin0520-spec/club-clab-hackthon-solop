# 프로젝트 개요
동아리 활동 기록 관리 웹앱.
동아리 임원이 활동 내역을 등록하고 조회·삭제할 수 있다.

팀: 1인 팀 (개인 학습용)
팀원: <본인 이름 입력>

# 기술 스택
- HTML / CSS / JavaScript (프레임워크 없음)
- 데이터 저장: 브라우저 localStorage
- 외부 라이브러리: 없음 (필요 시 CDN 방식으로 추가 검토)

# 파일 구조
- index.html : 화면 구조
- style.css  : 스타일
- app.js     : 기능 로직
<구조를 바꿨다면 실제 구조로 수정>

# 코딩 규칙
- 함수와 변수 이름은 영어 camelCase, 의미가 드러나게 작성
- 주요 함수 위에는 한 줄 한글 주석을 단다
- 하나의 함수는 하나의 일만 한다
- 들여쓰기는 스페이스 2칸

# 데이터 구조
활동 1건은 다음 형태로 저장한다.
{ id, title, date, place, memberCount, memo, createdAt }
localStorage 키: "activities"

# 작업 방식
- 코드를 작성하기 전에 무엇을 어떤 순서로 만들지 먼저 제안한다
- 한 번에 하나의 기능만 구현한다. 여러 기능을 동시에 만들지 않는다
- 기존에 동작하던 코드를 요청 없이 수정하거나 삭제하지 않는다
- 확실하지 않은 부분은 추측하지 말고 먼저 질문한다

# 하지 말 것
- 서버, 데이터베이스, 빌드 도구 도입 금지
- 요청하지 않은 파일 새로 생성 금지
- 사용하지 않는 예시 코드나 더미 데이터 대량 생성 금지
- 한 번에 100줄 넘는 코드를 통째로 출력하지 않기

# 커밋 규칙
- 기능 하나가 동작하면 바로 커밋한다
- 커밋 메시지 형식: <기능명>: <무엇을 했는지>
  예) 활동등록: 입력 폼과 저장 기능 구현

# 벤치마킹 참고 (웹서치 조사 결과 반영)
- 목록 아이템은 "활동명/날짜/장소" 한 줄 요약 + 클릭 시 참여인원·메모 펼침(아코디언) 형태로 정보 밀도를 유지한다.
- 삭제는 버튼 클릭 즉시 처리하지 않고 confirm() 등으로 1단계 확인을 거친다.
- 빈 목록 상태는 별도 이미지 없이 "아직 등록된 활동이 없어요" 텍스트 + 등록 버튼 강조로 처리한다.
- 검색/기간 필터는 목록 상단에 인라인으로 배치하고 실시간으로 필터링한다.
- 월별 통계는 그래프보다 "이번 달 활동 수 / 참여인원 합계·평균" 숫자 요약 카드가 구현 난이도 대비 효과적이다.
- JSON 내보내기는 Blob + `<a download>` 조합으로 간단히 구현한다.
- 수정 기능은 별도 화면 없이 카드 클릭 시 등록 폼에 값을 채우는 인라인 편집 방식을 쓴다.
- 화면 구성: 단일 페이지, 상단 등록 폼 → 검색/필터 바 → 하단 활동 카드 리스트(최신순) 순서로 배치한다. 탭 구조는 이 규모에서는 불필요하다.

# 현재 진행 상황
- [ ] 활동 등록
- [ ] 활동 목록 조회
- [ ] 활동 삭제
- [ ] 입력값 검증
- [ ] 선택 기능: <직접 기입>
